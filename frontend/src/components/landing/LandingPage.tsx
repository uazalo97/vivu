/**
 * ─────────────────────────────────────────────────────────────────────────
 * Landing page VinFast — TẠM ĐỂ TRẮNG theo yêu cầu (chưa thiết kế chi tiết).
 * Chatbox được nhúng độc lập qua <ChatWidget /> ở App.tsx, không phụ thuộc
 * nội dung landing page này. Khi thiết kế xong, thay nội dung tại đây.
 * ─────────────────────────────────────────────────────────────────────────
 */
export function LandingPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-white">
      {/* Placeholder — thay bằng nội dung landing page chính thức sau */}
      <p className="text-sm text-ink-soft/50">
        Landing page VinFast
      </p>
    </main>
  );
}
