# Base URL của backend API (không trailing slash).
# - Dev dùng Vite proxy nên để mặc định /api
# - Production nhúng vào trang khác origin: đổi sang full URL, kèm CORS phía backend
VITE_API_BASE=/api
