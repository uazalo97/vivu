# Pipeline làm sạch brochure (mục 02) — Python 3.11+
jinja2>=3.1
pyyaml>=6.0
python-dotenv>=1.0

# Vector DB + PostgreSQL client (local ingest)
qdrant-client>=1.9
psycopg2-binary>=2.9
sentence-transformers>=3.0

# (Tùy chọn, cho bước parse PDF thô sau này)
# pdfplumber>=0.11
# pymupdf>=1.24