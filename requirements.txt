# Runtime tối thiểu cho API (app/) — Python 3.11+
# Deploy PaaS/Docker chỉ cần file này. Pipeline/eval/tracing xem requirements-dev.txt.

# App (FastAPI + server)
fastapi>=0.104
uvicorn>=0.24
python-dotenv>=1.0

# PostgreSQL (app/core/db.py pool + decision.py data snapshot)
asyncpg>=0.29
psycopg2-binary>=2.9

# LLM
openai>=1.6

# Agent graph
langgraph>=0.2

# Redis (session store + tool-result cache)
redis>=5.0

# Qdrant REST client (app/core/retrieval.py QdrantREST dùng requests)
requests>=2.31

# Embedding score / rerank hybrid (app/agent/decision.py)
numpy>=1.26
