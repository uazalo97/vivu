# Pipeline clean + ingest — Python 3.11+
jinja2>=3.1
pyyaml>=6.0
python-dotenv>=1.0

# Vector DB + PostgreSQL client (local ingest)
qdrant-client>=1.9
psycopg2-binary>=2.9

# OpenRouter API (embedding + rerank + chat streaming)
requests>=2.31

# Backend FastAPI (SSE cho frontend)
fastapi>=0.115
uvicorn>=0.30
